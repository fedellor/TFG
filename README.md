En cada carpeta se encuentran implementaciones clásicas, del poroblema, así como las simulaciones con 6 qubits de cada uno de estes 3 algoritmos (con y sin ruido). 
También una gráfica de cada uno de los algoritmos probados comparados con el benchmark (algoritmo clásico). Además hay un .json en cada carpeta con métricas de resultados.

En la carpeta data están los datos usados, que se extrageron del dataset MNIST.
